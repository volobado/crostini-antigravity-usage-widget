name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    # Lagrange is Windows-only: it talks to Windows Credential Manager through
    # advapi32, so importing it anywhere else is meaningless.
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Compile
        run: python -m compileall -q lagrange scripts

      - name: Import every module
        # Nothing here touches the vault or the network at import time; if that
        # ever changes, this step is where it will show up.
        run: |
          python -c "import lagrange, lagrange.agylog, lagrange.api, lagrange.accounts, lagrange.chrome, lagrange.config, lagrange.credstore, lagrange.discovery, lagrange.doctor, lagrange.i18n, lagrange.runner, lagrange.screens, lagrange.session, lagrange.tokens, lagrange.tray, lagrange.ui; print(lagrange.__version__)"

      - name: The icon builds from code
        run: python scripts/make_icon.py

      - name: CLI responds without an install
        run: |
          python -m lagrange version
          python -m lagrange --help

      - name: The token ledger survives having nothing to read
        # No Antigravity on a runner means no conversation files; the ledger
        # must come back empty rather than raise.
        run: |
          python -c "from lagrange import agylog, tokens; print(agylog.conversations_dir()); print(tokens.sync()); print(tokens.summarise({'accounts': []}))"

      - name: Doctor runs with no Antigravity present
        # It must report the absence rather than crash — a broken environment is
        # exactly when people run it.
        run: python -m lagrange doctor --quick
        continue-on-error: true

      - name: Lint
        run: |
          python -m pip install --quiet ruff
          ruff check .

  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build
        run: |
          python -m pip install --quiet build
          python -m build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
